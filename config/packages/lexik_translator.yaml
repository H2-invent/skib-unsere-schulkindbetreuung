lexik_translation:
  fallback_locale: [de]         # (required) default locale(s) to use
  managed_locales: [de, fr, en] # (required) locales that the bundle has to manage
  base_layout: "@LexikTranslation/layout.html.twig"
  auto_cache_clean: false
  auto_cache_clean_interval: 600