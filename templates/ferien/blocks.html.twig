
<div class="modal-dialog modal-lg">
    <div class="modal-content">

<div class="modal-header">
    <h5 class="modal-title">{% trans %}Ferienblöcke zum Bestellen anklicken{% endtrans %}</h5>

</div>
<div class="modal-body">
<div class="row">

    {% for date in blocks %}

    <div class="text-left card-body" id="accordion">
        {% for b in date %}
            <div class="card mb-2">
                <div class="card-header" id="heading{{ loop.index }}">
                    <h5 class="mb-0">
                        <a data-toggle="collapse" data-target="#collapse{{ loop.index }}" aria-expanded="false" aria-controls="collapse{{ loop.index }}">
                            {{ b.minAlter }}
                        </a>
                    </h5>
               </div>

                <div id="collapse{{ loop.index }}" class="collapse" aria-labelledby="heading{{ loop.index }}" data-parent="#accordion">
                    <div class="card-body">
Text
                    </div>
                </div>
            </div>
        {% endfor %}
        {% endfor %}






</div>
    <div class="row">
        <div class="col-lg-12">
            <p>{% trans %}Preis für die Betreuung (incl. Vergünstigungen){% endtrans %}: <span class="price"></span></p>

        </div>
    </div>



</div>
        <div class="modal-footer">
            <button type="button" class="btn btn-outline-primary " data-dismiss="modal">{% trans %}Speichern{% endtrans %}</button>

        </div>

    </div>
    <script>
        // popovers Initialization
        $(function () {
            $('[data-toggle="popover"]').popover()
        })
    </script>
</div>