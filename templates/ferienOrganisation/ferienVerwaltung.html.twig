{% extends 'adminBase.html.twig' %}

{% block content %}
    <section class="dashboard-counts section-padding">
        <div class="container-fluid ">
            <div class="row">
                <!-- Count item widget-->
                <div class=" col-12 card">
                    <div class="card-header">
                        <h1> {% trans %}Ferienprogramm Verwaltung{% endtrans %} <a
                                    href="{{ path('org_admin_ferien_neu',{'id':org.id}) }}">
                                <button href="#" type="button" class="btn  bmd-btn-fab bmd-btn-fab-sm">
                                    <i class="material-icons">add</i>
                                </button>
                            </a></h1>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                            <tr>
                                <th></th>
                                <th>{% trans %}Titel{% endtrans %}</th>
                                <th>{% trans %}Anfangs Datum{% endtrans %}</th>
                                <th>{% trans %}End Datum{% endtrans %}</th>
                                <th>{% trans %}Anzahl{% endtrans %}</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for f in ferienblocks %}
                                <tr>
                                    <td>
                                        <div class="btn-group">
                                            <button class="btn bmd-btn-icon dropdown-toggle" type="button" id="ex2"
                                                    data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                <i class="material-icons">more_vert</i>
                                            </button>
                                            <div class="dropdown-menu dropdown-menu-left" aria-labelledby="ex2">
                                                <a class="dropdown-item"
                                                   href="#">{% trans %}Bearbeiten{% endtrans %}</a>
                                                <a class="dropdown-item"
                                                   href="#">{% trans %}Löschen{% endtrans %}</a>
                                            </div>
                                        </div>
                                    </td>
                                    <td>{{ f.translate.titel }}</td>
                                    <td>{{ f.startDate|date("d.m.Y") }}</td>
                                    <td>{{ f.endDate|date("d.m.Y") }}</td>
                                    <td>{{ f.maxAnzahl }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>
    </section>

{% endblock %}
