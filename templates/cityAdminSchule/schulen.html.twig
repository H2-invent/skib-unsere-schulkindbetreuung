{% extends 'adminBase.html.twig' %}

{% block content %}
<section class="dashboard-counts section-padding">
    <div class="container-fluid ">
        <div class="row">
            <!-- Count item widget-->
            <div class=" col-12 card">
                <div class="card-header">
                  <h1> {% trans %}Schulen{% endtrans %}  <a href="{{ path('city_admin_schule_new',{'id':city.id}) }}"><button href="#" type="button" class="btn  bmd-btn-fab bmd-btn-fab-sm">
                         <i class="material-icons">add</i>
                      </button></a></h1>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                        <tr>
                            <th></th>
                            <th>{% trans %}Name{% endtrans %}</th>
                            <th>{% trans %}Adresse{% endtrans %}</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for s in schulen %}
                            <tr>
                                <td>
                                    <div class="btn-group">
                                        <button class="btn bmd-btn-icon dropdown-toggle" type="button" id="ex2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                            <i class="material-icons">more_vert</i>
                                        </button>
                                        <div class="dropdown-menu dropdown-menu-left" aria-labelledby="ex2">
                                            <a class="dropdown-item" href="{{ path('city_admin_schule_edit',{'id':s.id}) }}">{% trans %}Bearbeiten{% endtrans %}</a>
                                            <a class="dropdown-item" href="{{ path('city_admin_schule_delete',{'id':s.id}) }}">{% trans %}Löschen{% endtrans %}</a>
                                        </div>
                                    </div>
                                </td>
                                <td>{{ s.name }}</td>
                                <td>{{ s.adresse }} {{ s.adresszusatz }}, {{ s.plz }} {{ s.ort }}</td>
                                </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>
</section>

{% endblock %}
