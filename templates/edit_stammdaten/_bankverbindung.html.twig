<div class="card">
    <div class="card-header">
        <h2>Bankverbindung</h2>
    </div>
    <div class="card-body">
        {{ form_row(form.iban) }}
        {{ form_row(form.bic) }}
        {{ form_row(form.kontoinhaber) }}
    </div>
</div>