{% extends 'adminBase.html.twig' %}

{% block content %}

    <section class="dashboard-counts section-padding">
        <div class="container">
            <div class="row">
                <div class="col-12 card">
                    <div class="card-header">
                        <div class="row">
                            <div class="col-12">
                                <h1>{% trans %}KVJS/Anfrage Gafög{% endtrans %}</h1>
                            </div>
                        </div>
                    </div>

                    <div class="card-body">


                        <div class="col-12">
                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>

        </div>

    </section>

{% endblock %}
