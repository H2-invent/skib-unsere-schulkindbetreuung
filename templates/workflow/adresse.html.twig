{% extends 'workflow/base.html.twig' %}
{% block content %}
        <h1 class="cover-heading">{% trans %}Daten der Eltern{% endtrans %}</h1>

    <ul>
        {% for error in errors %}
            <li>{{ error.message }}</li>
        {% endfor %}
    </ul>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}